interface:
  display_name: "Test UI"
  short_description: "Run fail-fast command/output UI tests"
  default_prompt: "Use $test-ui to run the command/output test cases in test/ui-test-plan.md and report the console session."
